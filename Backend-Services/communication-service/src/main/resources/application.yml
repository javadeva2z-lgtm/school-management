server:
  port: ${COMMUNICATION_SERVICE_PORT:8005}

spring:
  config:
    import: optional:classpath:/common-dev.yml,optional:classpath:/common-uat.yml,optional:classpath:/common-prod.yml,classpath:/common.yml
  application:
    name: communication-service
